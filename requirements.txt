# FootballVision Pro V3 - Python Dependencies
# For NVIDIA Jetson Orin Nano with JetPack 6.1+

# Core API Framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# System Monitoring
psutil>=5.9.0

# Metrics & Monitoring
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0

# Cloud Storage (R2 uploads)
boto3>=1.34.0

# GStreamer Python Bindings
# Note: Install via system package manager (apt)
# sudo apt install python3-gi gir1.2-gstreamer-1.0
# PyGObject is provided by python3-gi package
